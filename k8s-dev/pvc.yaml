apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mssql-pvc1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: ebs-csi-tracker # Change this to your storage class